# Changesets

This directory is used by [changesets](https://github.com/changesets/changesets) to manage versioning and changelogs.

## Adding a changeset

When you make a change that should result in a version bump:

```bash
npx changeset
```

Follow the prompts to describe your change. This creates a markdown file in this directory that will be consumed during the release process.
